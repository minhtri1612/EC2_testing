# Connection vars for group vpn_server (no IP here — IP passed by deploy.sh via -i "vpn_server,$OPENVPN_IP")
ansible_user: admin
ansible_ssh_private_key_file: ../terraform/ec2-bastion-vpn.pem
ansible_ssh_common_args: '-o StrictHostKeyChecking=no -o ConnectTimeout=30 -o AddressFamily=inet'
